/**
 * If changing this value, also change the value of the CSS var inside the root SCSS file
 */
export const INNER_CELL_SIZE = 70;
export const CELL_PADDING = 5;
export const CELL_SIZE = 80;
export const BRIDGE_WIDTH = 40;
