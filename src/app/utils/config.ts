/**
 * If changing this value, also change the value of the CSS var inside the root SCSS file
 */
export const INNER_CELL_SIZE = 60;
export const CELL_PADDING = 10;
export const CELL_SIZE = INNER_CELL_SIZE + 2 * CELL_PADDING;
export const BRIDGE_WIDTH = 30;
